export const recipesSection = document.querySelector('.recipes');

export const searchInputGlobal = document.querySelector('.search-input');

export const allCards = document.querySelectorAll('.card');

export const allTittles = document.querySelectorAll('h5');

export const allManual = document.querySelectorAll('.manual');

export const allIngredients = document.querySelectorAll('.card-text');
